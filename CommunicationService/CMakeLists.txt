include_directories(${CMAKE_SOURCE_DIR})

add_library(sensor_logger_communication_server SensorLoggerCommunicationServer.cpp)
target_link_libraries(sensor_logger_communication_server sensor_logger_communication)
target_link_libraries(sensor_logger_communication_server)

